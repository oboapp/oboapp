{
  "buildCommand": "mkdir -p public && echo '{}' > public/.keep.json",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["fra1"]
}
