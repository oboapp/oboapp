{
  "buildCommand": "pnpm run build && mkdir -p public && echo '{}' > public/_keep.json",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "regions": ["fra1"]
}
