import type { SourceDefinition } from "../source-definition";

export const sensorCommunity: SourceDefinition = {
  id: "sensor-community",
  url: "https://sensor.community/",
  name: "sensor.community",
  localities: ["bg.sofia"],
  experimental: true,
  emergent: true,
};
